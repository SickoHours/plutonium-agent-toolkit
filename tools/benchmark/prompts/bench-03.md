The file `<MOD_FF>` is a T6 fastfile built from the example mod. Using the toolkit, extract its
rawfile assets into a new directory under `<RUN>/bench-03-extract-rawfile/` and report the path
of each extracted file. Do not build anything.
