Compile the T6 server script at `<FIXTURE>/broken.gsc` with the toolkit, writing the job into a
new directory under `<RUN>/bench-01-compile-error/`. Report whether it compiled and, if not, the
compiler's first error line exactly as the toolkit reported it. Do not edit the script.
