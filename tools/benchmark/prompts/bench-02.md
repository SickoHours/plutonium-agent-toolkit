Build the example mod at `<REPO>/examples/hello-zm/project.json` with the toolkit and prove the
build: plan it, build it, and verify the build receipt including its inputs. Put every job into a
new directory under `<RUN>/bench-02-build-hello/`. Report the path and SHA-256 of the produced
`mod.ff` and what the receipts prove.
