"""Testing component: capture, run ledger and finite test recipes (Thread 2).

Status in this release: contracts registered, implementation planned. The
Windows capture backend (Windows.Graphics.Capture, WASAPI process loopback and
a hardware encoder) is a Stage 1 feasibility spike before any runner code.
"""
