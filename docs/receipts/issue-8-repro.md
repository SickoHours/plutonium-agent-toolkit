# Issue 8: native Windows experiment

Status: procedure only; not executed in this change. See [issue 8](https://github.com/SickoHours/plutonium-agent-toolkit/issues/8).

1. On the Windows qualification host, select the exact test package and start a private Town match from the Plutonium menu. Retain its package hash, version, map, initial log offset and screenshot. From inside that running match, run `pat game load-map town --output <new-job> --json`. Keep stdout and exit status; follow its exact receipt-bound check-load argv after loading settles. Record whether the match survives and whether a playable spawn is seen. Never replay uncertain delivery.
2. In a separate fresh process, compare a cold start using the fixed five-dvar sequence with a menu start. Town uses ui_zm_mapstartlocation=town, ui_gametype=zstandard, ui_zm_gamemodegroup=zsurvival, ui_mapname=zm_transit, g_gametype=zstandard. Verify current/latched gametype readback using the typed adapter before its single map submission. This experiment requires an explicitly admitted typed route; do not introduce raw console execution or treat this unqualified flow as supported.
3. Diff bounded console slices around `Searching for files required to download mod`, `partylobby.lua`, `Initializing game`, restart and shutdown. The existing issue records missing-weapon/IPAK lines in playable menu starts too; those lines alone do not establish the failure cause. Compare `hostquit` with `EXE_MATCHENDED` and retain delayed state, not only the initial sv_running value.
4. Save both original logs locally. Publish only sanitized receipt excerpts per this directory's README, with package identity, argv, exit status, observed survival, delayed check and explicit missing evidence. Remove personal paths, names, credentials and unrelated text. A procedure is not native or game qualification.
